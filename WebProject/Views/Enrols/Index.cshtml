@model IEnumerable<WebProject.Models.Enrol>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Students)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Courses)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Students.Student_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Courses.Course_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Enrol_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Enrol_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Enrol_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
